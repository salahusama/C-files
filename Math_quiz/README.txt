Works on WINDOWS only.

Compile with borland compiler as it contains flushall() function

This is a simple program with a menu - you can pick an option.

Randomly generates an eqution in the form:

x + y + z

the operations are also random